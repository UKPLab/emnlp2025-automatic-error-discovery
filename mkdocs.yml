site_name: arxiv2025_automatic_error_discovery
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2025_automatic_error_discovery
      handlers:
        python:
          options:
            docstring_style: numpy